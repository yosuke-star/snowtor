{
  "name": "django-project",
  "version": "1.0.0",
  "description": "Django project with Bootstrap",
  "scripts": {
    "build": "npm run build:css",
    "build:css": "sass static/scss:static/css"
  },
  "dependencies": {
    "bootstrap": "^5.3.2",
    "sass": "^1.71.1"
  }
}
